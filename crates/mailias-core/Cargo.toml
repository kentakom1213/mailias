[package]
name = "mailias-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Protocol implementation for deterministic mailias aliases"

[dependencies]
base64 = "0.22"
hmac = "0.12"
sha2 = "0.10"
subtle = "2.6"
thiserror = "2"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true

